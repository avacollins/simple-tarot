import { Meta, StoryObj } from '@storybook/addon-docs';
import * as SignupScreenStories from './signup-screen.stories';

<Meta of={SignupScreenStories} />

# SignupScreen Component

## Overview
The SignupScreen component is a full-screen layout that includes the SignupForm component. Signup screen will use the `useFirebaseAuth` hook to 
handle user registration.

<Canvas of={SignupScreenStories.Default} />