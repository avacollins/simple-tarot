import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SignupScreenStories from './signup-screen.stories';

<Meta of={SignupScreenStories.default} />

# SignupScreen Component

## Overview
The SignupScreen component is a full-screen layout that includes the SignupForm component. 
SignupScreen uses the `useSignupForm` hook to manage form state and client side validation.

<Canvas of={SignupScreenStories.Default} />