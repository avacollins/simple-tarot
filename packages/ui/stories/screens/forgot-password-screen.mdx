import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as ForgotPasswordScreen from './forgot-password-screen.stories';

<Meta of={ForgotPasswordScreen.default} />

# ForgotPasswordScreen Component

## Overview
The ForgotPasswordScreen component is a full-screen layout that includes the ForgotPasswordForm component. 
LoginScreen uses the `useForgotPasswordForm` hook to manage form state and client side validation.

<Canvas of={ForgotPasswordScreen.Default} />